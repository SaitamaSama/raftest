import * as React from 'react';
import { Panel } from '../../common/components/panel';

export interface ResultsProps {}

export const Results: React.FC<ResultsProps> = ({}): JSX.Element => {
  return <Panel></Panel>;
};
